{
  "name": "city-as-a-campus",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd ~/client && npm uninstall --save-dev gulp-sass && npm install --save-dev gulp-sass@2 && npm install && npm install -g gulp && gulp production"
  },
  "engines": {
    "node": "4.1.1",
    "npm": "2.1.x"
  }
}
